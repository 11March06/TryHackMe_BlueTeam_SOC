# 1. Definition of Log
- A "log" is a record of events, activities, or access attempts on a system. Logs are crucial for monitoring, detecting suspicious behavior, and investigating incidents. They track who logged in, when, from where, and what actions were performed.

# 2. Types of Logs

## Types of Logs

| Log Type | Definition | Examples | Purpose |
| --- | --- | --- | --- |
| Application logs | Records events inside specific software or services. | Errors in a banking app, Apache web requests. | Debugging, performance monitoring, detecting misuse. |
| System logs | Generated by the operating system to track core events. | Boot/shutdown records, driver failures. | Troubleshooting OS-level issues. |
| Infrastructure/Platform logs | Capture events from cloud platforms, containers, or virtualization layers. | Kubernetes pod crashes, AWS CloudWatch metrics. | Monitoring distributed systems and cloud health. |
| Access logs | Track who accessed what, when, and from where. | Web server logs showing IPs and URLs. | Detecting unauthorized access or brute-force attempts. |
| Security/Audit logs | Record authentication, permission changes, and compliance-related actions. | Windows Security Log, Linux /var/log/auth.log. | Intrusion detection, compliance (HIPAA, GDPR). |
| Event logs | General record of significant system or application events. | Windows Event Viewer entries. | Centralized monitoring of system health. |
| Trace logs | Very detailed logs used for debugging, often capturing step-by-step execution. | API call traces, stack traces in Java. | Deep troubleshooting and performance tuning. |

---

##  Linux Logs (specific locations)

- **Application logs** → `/var/log/appname/` (e.g., `/var/log/apache2/`)  
- **System logs** → `/var/log/syslog`, `/var/log/messages`  
- **Access logs** → `/var/log/apache2/access.log`, `/var/log/nginx/access.log`  
- **Security/Audit logs** → `/var/log/auth.log`, `/var/log/secure`  
- **Event logs** → Managed by `journalctl` (systemd journal)  
- **Trace logs** → Generated by applications (e.g., `strace`, debug mode)  

---

##  Windows Logs (specific locations)

- **Application logs** → Event Viewer → *Applications and Services Logs*  
- **System logs** → Event Viewer → *System*  
- **Access logs** → IIS access logs (default: `C:\inetpub\logs\LogFiles\`) or file access auditing  
- **Security/Audit logs** → Event Viewer → *Security*  
- **Event logs** → Event Viewer (all categories)  
- **Trace logs** → ETW (Event Tracing for Windows), debug logs (via Perfmon or PowerShell)  

---

##  Key Insight
- **Linux**: Uses plain text logs stored in `/var/log/`, flexible but requires parsing with tools like `grep`, `tail`, or `journalctl`.  
- **Windows**: Centralizes logs in **Event Viewer**, structured and queryable via GUI or PowerShell, often integrated with enterprise monitoring tools.  


## Structured Logging
<img width="1818" height="642" alt="image" src="https://github.com/user-attachments/assets/51797cfa-32e7-44ba-86ee-a75189062168" />

## Context / Correlation ID
<img width="1705" height="544" alt="image" src="https://github.com/user-attachments/assets/46c95950-6cf4-47a0-b90e-57b6f9196060" />

## Log level
<img width="1058" height="659" alt="image" src="https://github.com/user-attachments/assets/e11c0df7-1e3f-4c71-b0f8-ba75e6b72334" />


# 3.rom Raw Logs to Alerts in SIEM
a. Definition of Raw Logs
  - Raw logs are unprocessed event records generated by operating systems, applications, network devices, or security agents.
  - They contain details like timestamps, IP addresses, process IDs, user actions, and system events.

Example:
Windows Event Viewer entry: 4625 - Failed login attempt
Linux /var/log/auth.log: Failed password for root from 192.168.1.10

📍 Position: Raw logs are stored locally on endpoints (Windows/Linux), network devices (firewall, IDS), or applications (web/mail servers).

b. Normalization & Enrichment
  - Normalization: Convert raw logs into a structured format (JSON, ECS schema).
  - Enrichment: Add context (GeoIP, VirusTotal hash check, user identity).

Example:
Raw log: Failed password for root from 192.168.1.10
Normalized log:
   -     json
    {
      "event_type": "authentication_failure",
      "src_ip": "192.168.1.10",
      "username": "root",
      "geoip": "Vietnam"
    }

📍 Position: This happens in Logstash, Filebeat, Wazuh manager before logs enter the SIEM database.

c. Rule Application & Correlation
  - Rules define suspicious behaviors (failed logins, malware hash, persistence).
  - Correlation: Combine multiple conditions (≥3 constraints) to reduce false positives.

Example:
Rule 1: ≥5 failed logins in 1 minute
Rule 2: Login from unusual GeoIP
Rule 3: Account privilege escalation

→ Combined alert: “Account compromise suspected”

📍 Position: Rule engines run inside SIEM platforms (Splunk, Elastic Security, Security Onion, Wazuh).

d. Alert Generation
  - When rules match, an alert is created.
  - Alerts include severity, source, affected host, and recommended action.

Example:
Alert: “Privilege escalation attempt detected”
Severity: High
Source: Linux server 192.168.1.20
Action: Lock account, investigate logs

📍 Position: Alerts are stored in SIEM’s alert index and forwarded to SOC dashboards.

e. Dashboard Visualization
Dashboard shows alerts in charts, tables, timelines.

Functions:
Monitor system health (INFO/WARNING logs)
Detect attacks (ERROR/CRITICAL logs)
Provide SOC analysts with actionable intelligence

Example:
Pie chart: Distribution of log levels (INFO, WARNING, ERROR)
Timeline: Failed logins over 24h
Table: Top 10 source IPs triggering alerts

📍 Position: Dashboards live in Kibana (Elastic), Splunk UI, Security Onion console.
